version: '3.8'

services:
  app:
    build: .
    ports:
      - "80:3000"
    volumes:
      - ./data:/app/server/data
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - PORT=${PORT:-3000}
      - NODE_ENV=production
    restart: always
